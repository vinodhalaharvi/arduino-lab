#pragma once

/* Nothing exported here beyond led_coap_register(), declared in
   coap_server.h. Keeping this file so the source tree parallels the
   Thread node's layout — one .c/.h pair per domain. */
